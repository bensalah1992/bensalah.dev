<section id="contact" class="contact">
    <div class="container">
        <h2 class="section-title">Kontakt</h2>
        <p class="section-subtitle">Du willst mit mir arbeiten oder hast Fragen und Anregungen?<br>Dann schreib mir hier:</p>
        
        <div class="contact-container">
            <form 
                class="contact-form" 
                name="contact" 
                method="POST" 
                data-netlify="true"
            >
                <input type="hidden" name="form-name" value="contact" />
                
                <div class="form-group">
                    <input type="text" id="name" name="name" required placeholder="Dein Name" />
                    <label for="name">Name</label>
                </div>

                <div class="form-group">
                    <input type="email" id="email" name="email" required placeholder="Deine Mail" />
                    <label for="email">E-Mail</label>
                </div>

                <div class="form-group">
                    <textarea id="message" name="message" required placeholder="Was kann ich für dich tun?" rows="5"></textarea>
                    <label for="message">Nachricht</label>
                </div>

                <button type="submit" class="submit-btn">Nachricht senden</button>
            </form>
        </div>
    </div>
</section>

<style>
    .contact {
        padding: 4rem 0;
    }

    .section-title {
        text-align: center;
        margin-bottom: 1rem;
    }

    .section-subtitle {
        text-align: center;
        color: var(--secondary);
        margin-bottom: 4rem;
        font-size: 1.5rem;
    }

    .contact-container {
        max-width: 800px;
        margin: 0 auto;
    }

    .contact-form {
        background: var(--background);
        padding: 2rem;
        border-radius: 8px;
        border: 2px solid var(--primary);
        box-shadow: 0 0 20px rgba(255, 140, 0, 0.1);
    }

    .form-group {
        position: relative;
        margin-bottom: 2rem;
    }

    input, textarea {
        width: 100%;
        padding: 0.8rem;
        background: transparent;
        border: none;
        border-bottom: 2px solid var(--secondary);
        color: var(--text);
        font-family: inherit;
        font-size: 1.2rem;
        transition: all 0.3s ease;
        outline: none;
    }

    textarea {
        resize: vertical;
        min-height: 100px;
    }

    input:focus, textarea:focus {
        outline: none;
        border-bottom-color: var(--primary);
    }

    label {
        position: absolute;
        left: 0.8rem;
        top: -1.2rem;
        color: var(--secondary);
        transition: all 0.3s ease;
        pointer-events: none;
        font-size: 1.5rem;
    }

    input::placeholder,
    textarea::placeholder {
        color: rgba(255, 255, 255, 0.3);
        font-size: 1rem;
        opacity: 1;
        text-align: center;
        padding-top: 0.3rem;
    }

    .submit-btn {
        width: 100%;
        padding: 1rem;
        background: var(--primary);
        color: var(--background);
        border: none;
        border-radius: 4px;
        font-size: 1.1rem;
        cursor: pointer;
        transition: all 0.3s ease;
        text-transform: uppercase;
        font-family: inherit;
    }

    .submit-btn:hover {
        background: var(--accent);
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(255, 64, 129, 0.3);
    }

    @media (max-width: 768px) {
        .contact-form {
            padding: 1.5rem;
        }

        input, textarea {
            font-size: 1rem;
        }
    }
</style>