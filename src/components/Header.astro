<header class="header">
	<div class="container">
		<nav>
			<iframe 
				src="https://tryhackme.com/api/v2/badges/public-profile?userPublicId=2269152" 
				class="logo-badge"
				scrolling="no"
				title="TryHackMe Badge"
			></iframe>
			<ul class="nav-links">
				<li><a href="#projects">Projekte</a></li>
				<li><a href="#contact">Kontakt</a></li>
			</ul>
		</nav>
		<div class="hero">
			<h1>Willkommen zu bensalah.dev</h1>
			<p class="subtitle">Ich bin Abdallah Ben Salah <br>Ich baue Dinge für`s Internet und sorge für deine Cyber—Sicherheit<br>...und auch sonst ist es ganz nett hier</p>
		</div>
	</div>
</header>

<style>
	.header {
		padding: 2rem 0;
		background: linear-gradient(45deg, var(--background), #1a1a1a);
	}

	nav {
		display: flex;
		justify-content: space-between;
		align-items: center;
		margin-bottom: 4rem;
	}

	.logo-badge {
		border: none;
		width: 330px;
		height: 85px;
		overflow: hidden;
		display: block;
		margin-top: 8px;
		margin-bottom: 8px;
		outline: none;
	}

	.logo-badge:focus {
		outline: none;
	}

	.nav-links {
		display: flex;
		gap: 2rem;
		list-style: none;
	}

	.nav-links a {
		color: var(--text);
		text-decoration: none;
		font-size: 1.7rem;
		transition: color 0.3s ease;
	}

	.nav-links a:hover {
		color: var(--primary);
	}

	.hero {
		text-align: center;
		padding: 1rem 0 5rem 0;
	}
	.subtitle {
		font-size: 2rem;
		color: var(--secondary);
		margin-top: 3.5rem;
	}

	@media (max-width: 768px) {
		nav {
			flex-direction: column;
			gap: 1rem;
			text-align: center;
		}

		.nav-links {
			flex-direction: column;
			gap: 1rem;
		}

		.subtitle {
			font-size: 1.5rem;
			margin-top: 2.5rem;
		}
	}
</style>