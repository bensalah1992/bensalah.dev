// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Make sure there's nothing here that might interfere with CSS processing
});
